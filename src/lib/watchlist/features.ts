/**
 * Ticker watchlist UI (search ☆, /my tab) ships with public `/stocks/[ticker]` pages.
 * Schema and API stay in place; flip when stock pages + compare_tickers JSON are ready.
 */
export const WATCHLIST_TICKERS_UI_ENABLED = false;
